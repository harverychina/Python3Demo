# coding:utf8

"""
正则表达式库 re
特别正则运算符
导入
"""
# . ^ $ * + ? {m} {m,n} []
#  转义符 |   \d \D \s ()
# ^$
# .*?

import re
# 要匹配的字符
# p = re.compile('ca*t')
# 原字符点，任意字符
# p = re.compile('...')
# 以jpg结尾的字符串， $以什么结尾
# p = re.compile('jpg$')
# * 号，从0位置开始到位置为止
# p = re.compile('ca*t')
# p = re.compile('ca?t')
# p = re.compile('ca{4,6}t')
# p = re.compile('ca[bcd]t')

# p = re.compile('^$')

# 被匹配的字符串

# print(p.match('caaaat'))
# print(p.match('cabcdef'))
